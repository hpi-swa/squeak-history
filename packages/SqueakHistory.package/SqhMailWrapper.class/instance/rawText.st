accessing - domain
rawText
	"Answer the original sources of the message, possibly including MIME headers autc. Send #plainText for accessing the unformatted but readable message text instead."

	^ self mailMessage text